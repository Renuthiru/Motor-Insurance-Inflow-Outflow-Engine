# ============================================================
# .env.example — Safe template. Copy this to .env and fill in real values.
# NEVER commit .env to Git. Only commit this .env.example file.
# ============================================================

DB_HOST=localhost
DB_PORT=3306
DB_USER=your_mysql_username
DB_PASSWORD=your_mysql_password
DB_NAME=inflow_db
